UTest({
	'location': function(){
		
		ruta.add('/user/:id', assert.callback(function(data){
			eq(data.current.params.id, 'bob');
		}));
		
		ruta.navigate('/user/bob');
		
	},
	
	$after: function(){
		history.pushState({}, null, '/utest/');
	}
})
