
include
	.inject('/lib/ruta.js')
	.done(function(){
		
		
		UTest({
			'simple': function(){
				
				ruta.add('/user/:id', { any: true });
				
				
				var obj = ruta.get('/user/20');
				
				assert(obj != null);
				deepEq(obj.value, {any: true});
			}
		})
	
	
	});
	